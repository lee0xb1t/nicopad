# nicopad

A audio player

## If the font is not applying anti-aliasing, try running the following command and then retry.

windows:

```shell
set SLINT_BACKEND=winit-software
```

linux:

```shell
expert SLINT_BACKEND=winit-software
```
