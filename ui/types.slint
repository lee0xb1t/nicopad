export struct FavListModel {
    id: string,
    title: string,
}

export struct MediaListModel {
    bvid: string,
    title: string,
    duration: int,
    duration_mins: string,
    cover: string,
    cover-image: image,
    uid: string,
    uname: string,
}
